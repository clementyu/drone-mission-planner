{
  "name": "drone-mission-server",
  "version": "1.0.0",
  "description": "Backend for Drone Flight Editor",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "setup:env": "cp -n env-template.env .env || true",
    "dev": "concurrently \"npm start\" \"python ../python-backend/backend.py\""
  },
  "dependencies": {
    "@tmcw/togeojson": "^5.2.0",
    "@xmldom/xmldom": "^0.8.10",
    "axios": "^1.4.0",
    "dotenv": "^17.2.2",
    "express": "^4.18.2",
    "express-fileupload": "^1.4.0",
    "jszip": "^3.10.1"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
